syntax = "proto";
service Saudador {
  rpc DizerOla (SolicitacaoOla) returns (RespostaOla) {}
}

message SolicitacaoOla {        // estrutura do pacote
  string nome = 1;
}

message RespostaOla {
  string mensagem = 1;
}